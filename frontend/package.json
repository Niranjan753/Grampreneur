{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "export": "next build && next export",
    "deploy": "next build && next export && touch out/.nojekyll && gh-pages -d out -t true"
  },
  "dependencies": {
    "0g": "^0.4.2",
    "next": "14.2.7",
    "react": "^18",
    "react-dom": "^18",
    "vercel": "^37.2.1"
  },
  "devDependencies": {
    "postcss": "^8",
    "tailwindcss": "^3.4.1"
  }
}
